server:
  port: 80

spring:
  application:
    name: Movie-Ticket

  datasource:
    url: jdbc:mysql://localhost:3306/book_my_ticket?createDatabaseIfNotExist=true
    username: root
    password: yashu

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      url: redis://localhost:6379

admin:
  email: yashavanthakumarrt53@gmail.com
  password: Yashu